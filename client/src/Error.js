import React from 'react';

// Handle errors: Display a friendly msg
const Error = () => (
  <div className="ui red message">
    Sorry, something went wrong. We are working on getting this fixed as soon as we can.
  </div>
);

export default Error;
